- name: ansible-playbook
  hosts: aws-fontend
  tasks:
   - name: setting git
     shell: sudo apt update 
            sudo apt install git
            git --version
   - name: setting java
     shell: sudo apt install openjdk-11-jdk-headless
            java --version
  #  - name: create a file 
  #    command: touch /home/ubuntu/newfile
  #  - name: create a new directory
  #    command: mkdir /home//newdirectory